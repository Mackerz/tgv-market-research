# TMG Market Research Backend Environment Configuration

# Environment
ENVIRONMENT=development

# Authentication - JWT Secret Key (Required for session authentication)
# Generate using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-64-character-secret-key-here

# Authentication - API Key (Optional, for backward compatibility)
# Generate using: python -c "import secrets; print(secrets.token_hex(32))"
# Leave empty to disable authentication (development mode only!)
API_KEY=

# Google OAuth (Required for production Google SSO)
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=market_research
DB_USER=postgres
DB_PASSWORD=your_password

# Google Cloud Configuration
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
GCP_PROJECT_ID=your-project-id
GCS_BUCKET_NAME=your-bucket-name

# GCP Storage Buckets (Separate buckets for different media types)
GCP_STORAGE_ENABLED=true
GCP_STORAGE_BUCKET_PHOTOS=your-survey-photos-bucket
GCP_STORAGE_BUCKET_VIDEOS=your-survey-videos-bucket
GCP_STORAGE_BUCKET_QUESTION_MEDIA=your-question-media-bucket

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key

# CORS Configuration (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# File Upload Configuration
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,video/mp4,video/avi,video/mov

# Logging
LOG_LEVEL=INFO